Una opción para actualizar los submodulos de una clonación de un directorio git.

git submodule init
git submodule update --remote --recursive

# Ahora se procede a compilar con el método que se necesite.

./autogen.sh
make
